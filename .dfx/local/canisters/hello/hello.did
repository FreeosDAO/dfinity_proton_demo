service : {
  claim: (text) -> (text);
  fetchuser: (text) -> (text);
  get_balance: (text) -> (text);
  getnumusers: () -> (text);
  greet: (text) -> (text);
  inc: () -> (principal);
  storeuser: (text, text, text) -> (text);
  switchuser: (text, text) -> (text);
}
